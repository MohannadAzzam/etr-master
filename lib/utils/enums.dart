
enum MessageType { MESSAGE_FAILED, MESSAGE_SUCCESS }

enum DioType { post, get }

enum ErrorType { NO_INTERNET, CRASH_ERROR ,NOT_SUCCESS , NON}
enum FromType { waiting, all}
enum Gender {male, female }
enum CallType {voiceCall, videoCall }
enum CallBackStatus {delete, add , edit ,cancel,accept}
enum ItemState {Poshed, Error , Waiting}